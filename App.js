import { NavigationContainer } from "@react-navigation/native";
import { Auth } from "authentication";

const App = () => (
  <NavigationContainer>
    <Auth />
  </NavigationContainer>
);

export default App;
